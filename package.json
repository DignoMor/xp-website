{
  "name": "xp-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs tests/deploy/*.test.mjs",
    "test:local-build": "node --test tests/local-build.test.mjs",
    "test:docker-build": "node --test tests/docker-build.test.mjs",
    "test:deploy": "node --test tests/deploy/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "astro": "7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.6",
    "typescript": "5.9.2"
  }
}
